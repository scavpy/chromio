#:kivy 1.3.0

<IconImage>:
    source: "images/{0}{1}.png".format(root.skin, root.index)

<ColourButton>:
    IconImage:
        allow_stretch: True
        width: root.width - 10
        pos: root.x + 5, root.y + (root.height - self.height) / 2
        index: root.index
