#:kivy 1.3.0

<ColourButton>:
    Image:
        allow_stretch: True
        width: root.width - 10
        pos: root.x + 5, root.y + (root.height - self.height) / 2
        source: "images/button{0}.png".format(root.index)

<IconImage>:
    source: "images/button{0}.png".format(root.index)
